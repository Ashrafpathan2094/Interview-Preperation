# Normalize line endings: store text as LF in the repo (best for Linux/Vercel).
* text=auto eol=lf

# Treat common binary assets as binary (no EOL conversion).
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.woff binary
*.woff2 binary
