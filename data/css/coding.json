[
  {
    "id": "css-box-sizing-compute-width",
    "question": "A `.card` has `width: 300px; padding: 20px; border: 5px solid;`. What is its rendered width with the default `box-sizing`, and how do you make the rendered width stay exactly 300px? Show the fix.",
    "short": "**With the default `content-box`, the rendered width is content + padding + border = 300 + 40 + 10 = 350px; switch to `border-box` to keep the element at exactly 300px.**\n\n- **`content-box` math:** declared `width` (300px) + left/right padding (20+20) + left/right border (5+5) = **350px total**.\n- **`border-box` fix:** padding and border are drawn *inside* the declared width so the element renders at exactly 300px and the content area shrinks to 256px.\n- **Universal reset:** `*, *::before, *::after { box-sizing: border-box; }` is the near-universal best practice so all elements behave predictably.",
    "answer": "By default `box-sizing` is `content-box`, so the declared `width` applies only to the **content box**. Padding and border are added **outside** it:\n\n- content: `300px`\n- padding: `20px` left + `20px` right = `40px`\n- border: `5px` left + `5px` right = `10px`\n- **total rendered width = 350px**\n\nSwitching to `box-sizing: border-box` makes `width` include content + padding + border, so the element renders at exactly `300px` and the content area shrinks to absorb the padding/border. The universal `border-box` reset is a near-universal best practice.",
    "code": "/* Default content-box -> renders 350px wide */\n.card {\n  width: 300px;\n  padding: 20px;\n  border: 5px solid #333;\n}\n\n/* Fix: include padding + border inside the 300px */\n.card {\n  box-sizing: border-box; /* renders exactly 300px */\n}\n\n/* Common project-wide reset */\n*, *::before, *::after {\n  box-sizing: border-box;\n}",
    "codeLang": "css",
    "tags": ["box-model", "box-sizing", "layout"]
  },
  {
    "id": "css-specificity-which-color-wins",
    "question": "Given the HTML `<p id=\"intro\" class=\"text\">Hi</p>` and the rules below, what color is the text and why? Then explain how to override it without `!important`.",
    "short": "**The `#intro` ID selector wins with specificity `(1,0,0)`, beating `.text` `(0,1,0)` and `p` `(0,0,1)`, so the text is blue.**\n\n- **Why ID wins:** a single ID column always outranks any number of class or element selectors — they are in separate columns of the tuple.\n- **Source order is irrelevant here:** equal specificity would break the tie by order, but the scores are different so order doesn't matter.\n- **Clean override (no `!important`):** add the class alongside the ID — `#intro.text` scores `(1,1,0)` which beats `(1,0,0)` and lets you set a new color.",
    "answer": "Specificity is compared as `(IDs, classes, elements)`:\n\n- `p` -> `(0,0,1)`\n- `.text` -> `(0,1,0)`\n- `#intro` -> `(1,0,0)` **wins**\n\nSo the text renders **blue**. Source order only breaks ties between selectors of *equal* specificity.\n\nTo override cleanly without `!important`, raise specificity to match-or-beat the ID rule. `#intro.text` is `(1,1,0)` which beats `(1,0,0)`. Reaching for `!important` is discouraged because it starts an escalation war and is hard to override later.",
    "code": "p       { color: green; }   /* (0,0,1) */\n.text   { color: red;   }   /* (0,1,0) */\n#intro  { color: blue;  }   /* (1,0,0) -> WINS, text is blue */\n\n/* Clean override: higher specificity, no !important */\n#intro.text { color: black; } /* (1,1,0) beats (1,0,0) */",
    "codeLang": "css",
    "tags": ["specificity", "cascade", "important"]
  },
  {
    "id": "css-center-div-three-ways",
    "question": "Show three ways to horizontally and vertically center a child `.box` inside a `.parent`.",
    "short": "**Three reliable ways to center a child: flexbox on the parent, grid `place-items: center`, or absolute positioning with `transform` to compensate for the element's own size.**\n\n- **Flexbox:** `display: flex; justify-content: center; align-items: center;` on the parent — works for unknown child sizes and multiple children.\n- **Grid:** `display: grid; place-items: center;` — the shortest one-liner, `place-items` is shorthand for both axes.\n- **Absolute + transform:** parent gets `position: relative`; child uses `position: absolute; top: 50%; left: 50%; transform: translate(-50%, -50%)` — useful when you cannot change the parent's display.",
    "answer": "Three battle-tested centering techniques:\n\n1. **Flexbox** - the most common; works for unknown child sizes.\n2. **Grid** - `place-items: center` is the shortest one-liner.\n3. **Absolute + transform** - useful when the parent can't be a flex/grid container; `translate(-50%, -50%)` shifts the element back by half its own size so its center aligns with the parent's center.",
    "code": "/* 1) Flexbox */\n.parent { display: flex; justify-content: center; align-items: center; }\n\n/* 2) Grid */\n.parent { display: grid; place-items: center; }\n\n/* 3) Absolute + transform */\n.parent { position: relative; }\n.box {\n  position: absolute;\n  top: 50%;\n  left: 50%;\n  transform: translate(-50%, -50%);\n}",
    "codeLang": "css",
    "tags": ["centering", "flexbox", "grid", "positioning"]
  },
  {
    "id": "css-flexbox-shorthand-explain",
    "question": "Explain what `flex: 1 1 0` versus `flex: 0 0 200px` does on a flex item, and write a layout where a sidebar is a fixed 200px and the main area fills the rest.",
    "short": "**`flex` is shorthand for `flex-grow flex-shrink flex-basis`; `1 1 0` makes an item grow/shrink from zero (sharing space equally), while `0 0 200px` fixes the item at 200px with no flexibility.**\n\n- **`flex: 1 1 0`:** basis is `0` so the item ignores content size and takes an equal share of available space alongside siblings with the same value.\n- **`flex: 0 0 200px`:** grow and shrink are both `0` so the item is completely rigid at 200px regardless of available space.\n- **Classic pattern:** combine both — sidebar `flex: 0 0 200px` (fixed) + main `flex: 1 1 0` (fills rest) for a standard sidebar layout.\n- **Tip:** add `min-width: 0` to the flexible item to allow it to shrink below its content size.",
    "answer": "`flex` is shorthand for `flex-grow flex-shrink flex-basis`:\n\n- **`flex: 1 1 0`** -> grow factor 1, shrink factor 1, basis `0`. The item ignores its content size and shares free space equally with siblings of the same grow factor.\n- **`flex: 0 0 200px`** -> won't grow, won't shrink, basis fixed at `200px`. It stays exactly 200px regardless of available space.\n\nThe classic sidebar + main layout combines them: the sidebar is rigid, the main area absorbs everything else.",
    "code": ".layout { display: flex; }\n\n/* Fixed sidebar: never grows or shrinks */\n.sidebar { flex: 0 0 200px; }\n\n/* Main fills the remaining space */\n.main { flex: 1 1 0; min-width: 0; } /* min-width:0 lets it shrink below content */",
    "codeLang": "css",
    "tags": ["flexbox", "flex-grow", "flex-basis", "layout"]
  },
  {
    "id": "css-justify-vs-align-axis",
    "question": "In a flex container with `flex-direction: column`, which property controls vertical positioning and which controls horizontal? Write CSS to push items to the bottom and center them horizontally.",
    "short": "**`justify-content` always aligns along the main axis; `align-items` along the cross axis — and `flex-direction: column` swaps them so `justify-content` becomes vertical and `align-items` becomes horizontal.**\n\n- **Axis swap:** with `flex-direction: row` (default), main = horizontal; with `column`, main = vertical — the properties stay the same but they control different directions.\n- **Bottom + centered:** `justify-content: flex-end` pushes items to the bottom of the vertical main axis; `align-items: center` centers them on the horizontal cross axis.\n- **Common gotcha:** candidates apply `align-items` for vertical placement in a `row` container, then forget the swap when switching to `column`.",
    "answer": "`justify-content` always works along the **main axis**; `align-items` works along the **cross axis**. The direction of those axes depends on `flex-direction`:\n\n- `row` (default): main axis = horizontal, cross axis = vertical.\n- `column`: main axis = **vertical**, cross axis = **horizontal**.\n\nSo with `flex-direction: column`, to push items to the bottom you use `justify-content: flex-end` (main axis = vertical), and to center them horizontally you use `align-items: center` (cross axis = horizontal). This axis-swap trips up many candidates.",
    "code": ".container {\n  display: flex;\n  flex-direction: column;\n  height: 300px;\n  justify-content: flex-end; /* main axis = vertical -> bottom */\n  align-items: center;       /* cross axis = horizontal -> centered */\n}",
    "codeLang": "css",
    "tags": ["flexbox", "main-axis", "cross-axis", "alignment"]
  },
  {
    "id": "css-grid-responsive-auto-fit",
    "question": "Write a responsive card grid where each card is at least 200px wide, columns fill the row, and the grid reflows automatically without media queries. Explain `auto-fit` vs `auto-fill`.",
    "short": "**`repeat(auto-fit, minmax(200px, 1fr))` creates a fully responsive column grid with no media queries — columns reflow automatically as the container resizes.**\n\n- **`minmax(200px, 1fr)`:** each column is at least 200px and can grow to share remaining space equally.\n- **`auto-fit` vs `auto-fill`:** both create as many tracks as fit, but `auto-fit` collapses empty trailing tracks to zero so existing items **stretch to fill the row**; `auto-fill` preserves empty tracks, leaving gaps.\n- **Choose `auto-fit`** when you want items to expand; choose `auto-fill` when you want a stable column rhythm even with sparse items.",
    "answer": "`repeat(auto-fit, minmax(200px, 1fr))` is the canonical responsive grid:\n\n- `minmax(200px, 1fr)` -> each column is at least `200px` and grows up to an equal `1fr` share.\n- The grid recalculates how many columns fit as the container resizes, with no media queries.\n\n**`auto-fit` vs `auto-fill`:** When there are fewer items than the row can hold, `auto-fill` creates **empty phantom tracks** (items stay their min size, trailing gaps appear), while `auto-fit` **collapses** those empty tracks to `0` so the existing items stretch to fill the whole row. Use `auto-fit` when you want items to expand; `auto-fill` when you want a stable column rhythm.",
    "code": ".grid {\n  display: grid;\n  grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));\n  gap: 16px;\n}\n\n/* Swap to auto-fill if you want empty trailing tracks preserved:\n   grid-template-columns: repeat(auto-fill, minmax(200px, 1fr)); */",
    "codeLang": "css",
    "tags": ["grid", "auto-fit", "auto-fill", "responsive", "fr"]
  },
  {
    "id": "css-grid-template-areas",
    "question": "Build a page layout (header, sidebar, main, footer) using `grid-template-areas`. Show the named-area CSS.",
    "short": "**`grid-template-areas` lets you declare a layout as a readable ASCII map — each quoted string is a row, repeated names span cells, and children opt in by setting `grid-area` to their name.**\n\n- **Setup:** assign `grid-area: name` to each child, then paint rows as quoted strings on the container in `grid-template-areas`.\n- **Spanning:** repeating the same name across adjacent cells spans that region; a `.` marks an empty cell; named regions must be rectangular.\n- **Responsive power:** restructure the entire layout in a media query by simply rewriting the area map — no need to touch the children's CSS.",
    "answer": "`grid-template-areas` lets you describe the layout as an **ASCII map**, which is highly readable. Steps:\n\n1. Give each child a name via `grid-area`.\n2. On the container, declare the column/row track sizes.\n3. Paint the grid with quoted rows in `grid-template-areas`; repeating a name spans that cell across tracks.\n\nUse a `.` to leave a cell empty. This makes responsive restructuring trivial - you just rewrite the area map inside a media query.",
    "code": ".page {\n  display: grid;\n  grid-template-columns: 200px 1fr;\n  grid-template-rows: auto 1fr auto;\n  grid-template-areas:\n    \"header  header\"\n    \"sidebar main\"\n    \"footer  footer\";\n  min-height: 100vh;\n  gap: 12px;\n}\n.header  { grid-area: header; }\n.sidebar { grid-area: sidebar; }\n.main    { grid-area: main; }\n.footer  { grid-area: footer; }",
    "codeLang": "css",
    "tags": ["grid", "grid-template-areas", "layout"]
  },
  {
    "id": "css-position-sticky-header",
    "question": "Make a section header stick to the top of the viewport while its section is scrolling, then release when the next section arrives. Explain why `position: sticky` sometimes silently fails.",
    "short": "**`position: sticky` with `top: 0` makes an element scroll normally then pin at the top — but it silently fails when ancestor overflow, missing insets, or insufficient parent height block it.**\n\n- **How it works:** behaves like `relative` until the scroll threshold (`top: 0`) is hit, then sticks like `fixed` within its scroll container, releasing when the parent scrolls out of view.\n- **Failure: no inset declared** — sticky requires at least one of `top`/`right`/`bottom`/`left` to know where to stick.\n- **Failure: ancestor `overflow`** — any ancestor with `overflow: hidden`, `auto`, or `scroll` clips the sticky containing block, breaking the effect.\n- **Failure: parent too short** — if the parent isn't taller than the sticky element, there is no room to scroll over.",
    "answer": "`position: sticky` is a hybrid: the element behaves like `relative` until its scroll offset crosses the threshold you set (e.g. `top: 0`), then it behaves like `fixed` **within its parent's box**, releasing when the parent scrolls past.\n\n**Common silent failures:**\n- No inset declared - sticky needs at least one of `top/right/bottom/left`.\n- An ancestor has `overflow: hidden`, `auto`, or `scroll`, which clips the sticky containing block.\n- The sticky element's parent isn't tall enough to scroll, so there's nothing to stick over.\n- A fixed-height ancestor smaller than the content.",
    "code": ".section-header {\n  position: sticky;\n  top: 0;        /* REQUIRED inset or it won't stick */\n  background: #fff;\n  z-index: 10;\n}\n\n/* Gotcha: this on any ancestor breaks stickiness */\n.some-wrapper { overflow: hidden; } /* remove/change to overflow: visible */",
    "codeLang": "css",
    "tags": ["positioning", "sticky", "scroll"]
  },
  {
    "id": "css-zindex-stacking-context",
    "question": "An element with `z-index: 9999` still renders behind a sibling with `z-index: 2`. The HTML and CSS look correct. What's the likely cause and how do you debug/fix it?",
    "short": "**`z-index` is not global — it only orders elements within the same stacking context, so `z-index: 9999` on a child is meaningless if its parent forms a lower-priority stacking context.**\n\n- **Stacking context triggers:** `position` + `z-index` (non-`auto`), `opacity < 1`, `transform`, `filter`, `will-change`, flex/grid item with `z-index`, `isolation: isolate`, among others.\n- **The trap:** if the `9999` element's parent creates its own context at a low z-index, the child is painted as part of that parent — it cannot escape to beat elements in a higher sibling context.\n- **Fix options:** raise the trapping parent's `z-index`, remove the property that created the unwanted context, or restructure the DOM so the element is at the right level.",
    "answer": "`z-index` is **not global** - it only orders elements **within the same stacking context**. If the `9999` element lives inside a parent that forms its own stacking context, that whole parent is ordered as a single unit against the `z-index: 2` sibling's context. So a child's 9999 can't escape a parent that sits lower.\n\n**Triggers that create a new stacking context:** `position` other than static **with** a `z-index`, `opacity < 1`, any `transform`/`filter`/`perspective`, `will-change`, `isolation: isolate`, flex/grid children with `z-index`, etc.\n\n**Fix:** raise the `z-index` on the *parent* context (or remove the property that created the trapping context), rather than inflating the child's value.",
    "code": "/* Parent accidentally creates a stacking context via opacity */\n.modal-wrapper {\n  opacity: 0.99;     /* <- creates a stacking context, traps children */\n  z-index: 1;\n}\n.modal { z-index: 9999; } /* only relative to .modal-wrapper */\n\n.toast { position: relative; z-index: 2; } /* still wins vs the wrapper */\n\n/* Fix: lift the parent's context, or drop opacity */\n.modal-wrapper { opacity: 1; z-index: 10; }",
    "codeLang": "css",
    "tags": ["z-index", "stacking-context", "positioning", "debugging"]
  },
  {
    "id": "css-mobile-first-media-query",
    "question": "Write mobile-first CSS for a layout that is a single column on phones and becomes two columns at 768px and up. Why is mobile-first preferred?",
    "short": "**Mobile-first means base styles target the smallest screen, and `@media (min-width: ...)` queries layer progressively richer layouts — so phones only parse what they need.**\n\n- **Base = mobile:** un-queried CSS provides the simplest layout (single column, minimal rules); media queries only add what larger screens can accommodate.\n- **`min-width` is additive:** each breakpoint only adds or overrides what changes — cleaner and easier to reason about than desktop-first `max-width` overrides.\n- **Performance:** constrained phones on slow networks don't parse desktop rules they'll never apply — the base stays lean.\n- **Pattern:** start with `grid-template-columns: 1fr`; at `min-width: 768px` switch to two columns.",
    "answer": "**Mobile-first** means the default (un-queried) styles target the smallest screen, and you add capability with `@media (min-width: ...)` as the viewport grows. Contrast with desktop-first, which starts large and strips down via `max-width`.\n\n**Why preferred:**\n- The base stylesheet stays lean - low-powered phones don't parse desktop-only rules they won't use.\n- Progressive enhancement: you add complexity only when there's room for it.\n- `min-width` queries cascade cleanly upward and are easier to reason about than overlapping `max-width` ranges.",
    "code": "/* Base = mobile: single column */\n.grid {\n  display: grid;\n  grid-template-columns: 1fr;\n  gap: 16px;\n}\n\n/* Enhance for tablets and up */\n@media (min-width: 768px) {\n  .grid { grid-template-columns: 1fr 1fr; }\n}",
    "codeLang": "css",
    "tags": ["responsive", "media-queries", "mobile-first"]
  },
  {
    "id": "css-rem-em-difference",
    "question": "Explain the difference between `em` and `rem`, then predict the computed `font-size` and `padding` for the nested elements in the snippet below.",
    "short": "**`rem` is relative to the root `html` font-size and never compounds; `em` is relative to the element's own font-size and multiplies with each nesting level.**\n\n- **`rem` (root-relative):** `1rem` = `html` font-size (typically 16px) everywhere — no compounding, consistent, and respects user browser settings.\n- **`em` (element-relative):** for `font-size` it inherits from the parent; for other properties it references the element's own font-size — nested `1.5em` inside `1.5em` = `1.5 × 1.5 = 2.25×` the root.\n- **Worked example:** root 16px → `.child` `1.5em` = **24px** → `.grandchild` `1.5em` = **36px** (compounding); `.grandchild` `padding: 1em` = **36px**.",
    "answer": "- **`rem`** = relative to the **root** element's font-size (`html`). It never compounds, which makes spacing predictable.\n- **`em`** = relative to the **element's own** `font-size` (for the `font-size` property itself it's relative to the *parent's* font-size). When set on nested elements it **multiplies**.\n\nWalking the snippet (root = 16px):\n- `.child` `font-size: 1.5em` -> 1.5 * 16 = **24px**.\n- `.grandchild` `font-size: 1.5em` -> 1.5 * 24 = **36px** (compounding).\n- `.grandchild` `padding: 1em` -> 1 * 36 = **36px** (em of its own font-size).\n- A `2rem` value anywhere = 2 * 16 = **32px**, regardless of nesting.",
    "code": "html { font-size: 16px; }\n\n.child      { font-size: 1.5em; } /* 24px */\n.grandchild {\n  font-size: 1.5em;  /* 1.5 * 24 = 36px (compounds) */\n  padding: 1em;      /* 1 * 36 = 36px */\n}\n\n.anywhere   { margin: 2rem; } /* 2 * 16 = 32px, no compounding */",
    "codeLang": "css",
    "tags": ["units", "em", "rem", "inheritance"]
  },
  {
    "id": "css-pseudo-element-required-asterisk",
    "question": "Add a red asterisk after every required form label without changing the HTML, using `::after`. Also explain why `content` is mandatory.",
    "short": "**Target `.label.required::after`, set `content: \" *\"` and `color: red` — pseudo-elements only render when the `content` property is explicitly declared, even if its value is an empty string.**\n\n- **`content` is mandatory:** without it (even `content: \"\"`), the `::after` pseudo-element is not generated and nothing renders.\n- **Selector:** `.label.required::after` combines the label class and required class so only required labels get the asterisk — no HTML changes needed.\n- **Accessibility note:** screen readers may read the `content` value, so keep it minimal and purely decorative when possible; use `aria-required` for semantic required state.",
    "answer": "`::before` and `::after` inject generated content as the first/last child of the element, styleable like a real box but absent from the DOM/HTML. They are perfect for decorative markers like a required asterisk.\n\n**`content` is mandatory** - without it (even `content: \"\"`) the pseudo-element is not generated and nothing renders. For purely decorative content, an empty string is common; for meaningful glyphs, put the character in `content`. Note that screen readers may read pseudo-element text, so don't rely on it for critical semantics.",
    "code": ".label.required::after {\n  content: \" *\";       /* REQUIRED, or nothing renders */\n  color: red;\n  font-weight: bold;\n}\n\n/* Decorative-only example */\n.icon::before {\n  content: \"\";          /* still required, even if empty */\n  display: inline-block;\n  width: 16px; height: 16px;\n  background: url('star.svg');\n}",
    "codeLang": "css",
    "tags": ["pseudo-elements", "before-after", "content"]
  },
  {
    "id": "css-truncate-ellipsis-single-multiline",
    "question": "Write CSS to truncate a single line of text with an ellipsis, then truncate multi-line text to 3 lines with an ellipsis.",
    "short": "**Single-line truncation requires three cooperating properties; multi-line truncation uses the `-webkit-line-clamp` technique — all variants need `overflow: hidden`.**\n\n- **Single-line:** `white-space: nowrap` (prevent wrapping) + `overflow: hidden` (clip) + `text-overflow: ellipsis` (draw `...`) — all three are required; remove any one and it breaks.\n- **Multi-line (`-webkit-line-clamp`):** `display: -webkit-box; -webkit-box-orient: vertical; -webkit-line-clamp: N; overflow: hidden` — clamps to N lines with an ellipsis; widely supported.\n- **Standard property:** `line-clamp: N` is now the spec-standard equivalent but still benefits from the webkit version as a fallback.",
    "answer": "**Single-line truncation** needs three properties together - prevent wrapping, clip the overflow, then render the ellipsis:\n\n- `white-space: nowrap` keeps it on one line.\n- `overflow: hidden` clips the excess.\n- `text-overflow: ellipsis` draws the `...`.\n\n**Multi-line truncation** uses the widely supported line-clamp pattern: `display: -webkit-box` with `-webkit-box-orient: vertical` and `-webkit-line-clamp: N`. Modern browsers also support the standard `line-clamp`. The element still needs `overflow: hidden`.",
    "code": "/* Single line */\n.truncate {\n  white-space: nowrap;\n  overflow: hidden;\n  text-overflow: ellipsis;\n}\n\n/* Multi-line: clamp to 3 lines */\n.clamp-3 {\n  display: -webkit-box;\n  -webkit-box-orient: vertical;\n  -webkit-line-clamp: 3;\n  line-clamp: 3;          /* standard */\n  overflow: hidden;\n}",
    "codeLang": "css",
    "tags": ["overflow", "text-overflow", "ellipsis", "truncation"]
  },
  {
    "id": "css-keyframes-spinner",
    "question": "Create a smooth, infinitely rotating loading spinner using `@keyframes`. Explain why `transform` is preferred over animating `top`/`left`.",
    "short": "**Define a `@keyframes spin` rotating from `0deg` to `360deg` and apply it with `animation: spin 1s linear infinite` — use `transform` instead of `top`/`left` so the animation is GPU-composited and smooth.**\n\n- **`@keyframes` binding:** `animation: <name> <duration> <timing> <iteration>` — `linear` keeps speed constant; `infinite` loops forever.\n- **Why `transform` over positional properties:** `top`/`left`/`margin` trigger a full **reflow** every frame; `transform: rotate()` is handled by the compositor thread, skipping layout and paint entirely.\n- **Spinner anatomy:** a circle via `border-radius: 50%` with one border side colored differently rotates to create the illusion of a spinning arc.",
    "answer": "Define the animation with `@keyframes`, then bind it via the `animation` shorthand (`name duration timing-function iteration-count`). `linear` keeps rotation speed constant; `infinite` loops forever.\n\n**Why `transform` over `top`/`left`:** animating geometric properties like `top`, `left`, `width`, or `margin` triggers **layout (reflow)** and then **paint** on every frame, which is expensive. `transform` (and `opacity`) can be handled by the **compositor** thread - the browser skips layout and paint, producing smooth 60fps animation. This reflow-vs-repaint distinction is a frequent follow-up.",
    "code": "@keyframes spin {\n  from { transform: rotate(0deg); }\n  to   { transform: rotate(360deg); }\n}\n\n.spinner {\n  width: 40px; height: 40px;\n  border: 4px solid #ddd;\n  border-top-color: #3498db;\n  border-radius: 50%;\n  animation: spin 1s linear infinite;\n}",
    "codeLang": "css",
    "tags": ["animations", "keyframes", "transform", "performance", "reflow-repaint"]
  },
  {
    "id": "css-variables-theme-toggle",
    "question": "Use CSS custom properties to define a theme, then override it for a dark theme by toggling a class on `<html>`. Show `var()` with a fallback.",
    "short": "**Declare design tokens as custom properties on `:root`, consume them with `var()`, and override the same variables under a `.dark` class — everything using `var()` updates automatically.**\n\n- **Cascade and inherit:** custom properties flow down the DOM like normal properties, so overriding `--bg` on `html.dark` re-themes all descendants with zero per-element rules.\n- **`var()` fallback:** `var(--accent, #2980b9)` supplies a default if `--accent` is unset — useful for optional tokens.\n- **Runtime flip:** adding/removing the `.dark` class on `<html>` via JS instantly swaps the entire palette without re-downloading or recomputing stylesheets.\n- **Contrast with Sass:** Sass `$vars` are resolved at compile time and cannot do live theming.",
    "answer": "Custom properties are declared with a `--` prefix and read with `var()`. They **inherit** and **cascade** like normal properties, so re-declaring them on a more specific scope overrides them for that subtree.\n\nPattern:\n1. Define defaults on `:root`.\n2. Consume with `var(--token, fallback)` - the fallback is used if the variable is undefined.\n3. Add a theme class (e.g. `.dark` on `<html>`) that **redefines** the same variables; everything using `var()` updates instantly, no per-element overrides needed.",
    "code": ":root {\n  --bg: #ffffff;\n  --fg: #111111;\n  --accent: #3498db;\n}\n\n.dark {\n  --bg: #111111;\n  --fg: #f5f5f5;\n}\n\nbody {\n  background: var(--bg);\n  color: var(--fg);\n}\n.btn {\n  /* fallback used only if --accent is undefined */\n  background: var(--accent, #2980b9);\n}",
    "codeLang": "css",
    "tags": ["custom-properties", "variables", "theming", "cascade"]
  },
  {
    "id": "css-transition-smooth-hover",
    "question": "A button changes color instantly on hover. Make the change animate smoothly over 200ms, and explain why putting the `transition` on `:hover` instead of the base selector is a common mistake.",
    "short": "**Declare `transition` on the base selector (`.btn`), not inside `.btn:hover` — putting it only on `:hover` means the transition rule vanishes on hover-out, causing the value to snap back instantly.**\n\n- **Why base selector:** the `transition` property must be present in *both* states (enter and exit) to animate in both directions; the base class is always applied.\n- **Hover-only mistake:** when the rule is only in `:hover`, the animation plays while hovering, but the moment you leave the transition is gone and the style reverts without interpolation.\n- **Good properties to transition:** `color`, `background`, `opacity`, `transform` — cheap; avoid `width`/`height`/`top` which trigger reflow every frame.",
    "answer": "`transition` interpolates between the current and target value when a property changes. The shorthand is `transition: <property> <duration> <timing-function> <delay>`.\n\n**Common mistake:** declaring `transition` inside `.btn:hover`. Then the transition rule only exists *while hovering*, so the hover-in animates, but on hover-out the rule disappears and the value **snaps back instantly**. Put the `transition` on the **base** selector so it applies in both directions. Also prefer transitioning cheap properties (`color`, `background`, `transform`, `opacity`).",
    "code": "/* Correct: transition on the base selector */\n.btn {\n  background: #3498db;\n  transition: background 200ms ease-in-out;\n}\n.btn:hover {\n  background: #2c80b4;\n}\n\n/* Wrong: only animates hover-in, snaps back on hover-out\n.btn:hover { transition: background 200ms; } */",
    "codeLang": "css",
    "tags": ["transitions", "hover", "animation"]
  },
  {
    "id": "css-bem-naming-component",
    "question": "Refactor an ambiguous card component to BEM naming. What problem does BEM solve and what do the `__` and `--` separators mean?",
    "short": "**BEM (Block, Element, Modifier) is a naming convention that produces single-class selectors with flat, predictable specificity, preventing naming collisions and making markup self-documenting.**\n\n- **Block (`.card`):** a standalone, reusable component with its own meaning.\n- **Element (`.card__title`):** a part of the block joined with `__` — has no meaning outside the block.\n- **Modifier (`.card--featured`, `.card__button--disabled`):** a variant or state joined with `--` — applied alongside the base class.\n- **Benefit:** every selector is a single class at specificity `(0,1,0)` — overrides are simple, no `!important` needed, and class names reveal component structure at a glance.",
    "answer": "**BEM** (Block, Element, Modifier) is a naming convention that makes CSS predictable and scoped without relying on selector nesting:\n\n- **Block** - a standalone component: `.card`.\n- **Element** - a part of a block, joined with `__`: `.card__title`, `.card__body`.\n- **Modifier** - a variant/state, joined with `--`: `.card--featured`, `.card__button--disabled`.\n\n**Problems it solves:** avoids deep descendant selectors (keeps specificity flat at one class = `0,1,0`), prevents naming collisions, and makes the markup self-documenting. You style by single class, so overrides stay simple and there's no specificity creep.",
    "code": "<!-- BEM markup -->\n<div class=\"card card--featured\">\n  <h2 class=\"card__title\">Title</h2>\n  <p class=\"card__body\">Text</p>\n  <button class=\"card__button card__button--disabled\">Buy</button>\n</div>",
    "codeLang": "html",
    "tags": ["bem", "naming", "architecture", "specificity"]
  },
  {
    "id": "css-display-none-vs-visibility",
    "question": "What is the difference between `display: none`, `visibility: hidden`, and `opacity: 0` for hiding an element? Describe layout impact, interactivity, and accessibility for each.",
    "short": "**`display: none` removes the element from layout and the a11y tree; `visibility: hidden` keeps its space but is non-interactive; `opacity: 0` keeps space, interactivity, and a11y presence.**\n\n- **`display: none`:** no box, no space, removed from the accessibility tree; toggling triggers reflow; cannot be transitioned.\n- **`visibility: hidden`:** occupies space, non-interactive, hidden from screen readers; child elements can override with `visibility: visible`; transitionable as a discrete step.\n- **`opacity: 0`:** fully transparent but still in layout — **still clickable, still focusable, still read by AT** (a common bug); add `pointer-events: none` to disable clicks.\n- **Smooth hide pattern:** combine `opacity: 0` + `visibility: hidden` and transition both — fades visually and removes interactivity.",
    "answer": "All three hide an element visually but behave very differently:\n\n- **`display: none`** - element is removed from the render tree: takes **no space**, can't be clicked or focused, removed from the accessibility tree. Toggling it triggers reflow. Not transitionable.\n- **`visibility: hidden`** - element still **occupies its space** (layout unchanged), but is not painted, not clickable, and hidden from screen readers. Transitionable as a discrete step.\n- **`opacity: 0`** - element is fully transparent but **still there**: occupies space, **still captures clicks**, and is **still announced** by screen readers. Smoothly transitionable (compositor-friendly). Add `pointer-events: none` if you need to disable clicks while fading.",
    "code": ".gone    { display: none; }       /* no space, no a11y, no events */\n.hidden  { visibility: hidden; }   /* keeps space, no events, no a11y */\n.transparent {\n  opacity: 0;                      /* keeps space, STILL clickable + read */\n  pointer-events: none;            /* add this to disable interaction */\n}",
    "codeLang": "css",
    "tags": ["display", "visibility", "opacity", "accessibility"]
  }
]
