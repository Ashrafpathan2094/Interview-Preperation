[
  {
    "id": "html-semantic-elements-why",
    "question": "What are semantic HTML elements and why do they matter?",
    "short": "**Semantic elements like `<header>`, `<nav>`, `<main>`, `<article>`, `<section>`, `<aside>`, and `<footer>` describe the *meaning* of content rather than its appearance.**\n\n- **Accessibility:** they map to ARIA landmarks (`<nav>` → `navigation`, `<main>` → `main`), letting screen-reader users jump directly between page regions.\n- **SEO:** search engines use the document structure to infer content hierarchy and importance.\n- **Maintainability:** self-documenting markup is far easier to read than a sea of `<div>` containers.",
    "answer": "**Semantic elements** convey the *meaning and role* of their content to browsers, assistive technologies, and search engines, unlike generic containers like `<div>`/`<span>` which carry no meaning.\n\n**Why they matter:**\n- **Accessibility** - Many map to ARIA *landmarks* (`<nav>` -> `navigation`, `<main>` -> `main`, `<aside>` -> `complementary`), letting screen-reader users jump directly between regions.\n- **SEO** - Search engines infer content hierarchy and importance from structure.\n- **Maintainability** - Self-documenting markup is easier to read than a sea of `<div>`s.\n\n**Common landmarks:**\n- `<header>` - introductory content / banner.\n- `<nav>` - major navigation block.\n- `<main>` - the single primary content region (one per page).\n- `<article>` - self-contained, independently distributable content (a blog post, comment, card).\n- `<section>` - a thematic grouping, usually with a heading.\n- `<aside>` - tangential content (sidebars, pull quotes).\n- `<footer>` - footer for its nearest sectioning ancestor.\n\n**Rule of thumb:** use `<article>` when the content would make sense on its own (e.g. in an RSS feed); use `<section>` to group related content under a heading.",
    "code": "<body>\n  <header>...</header>\n  <nav aria-label=\"Primary\">...</nav>\n  <main>\n    <article>\n      <h1>Post title</h1>\n      <section><h2>Intro</h2>...</section>\n    </article>\n    <aside>Related links</aside>\n  </main>\n  <footer>...</footer>\n</body>",
    "codeLang": "html",
    "tags": ["semantic", "accessibility", "seo", "structure"]
  },
  {
    "id": "html-section-vs-article-vs-div",
    "question": "What is the difference between <section>, <article>, and <div>?",
    "short": "**`<article>` is self-contained distributable content, `<section>` is a thematic grouping with a heading, and `<div>` is a non-semantic layout/scripting hook with no meaning.**\n\n- **`<article>`:** stands alone and still makes sense out of context — a blog post, comment, or product card.\n- **`<section>`:** groups related content under a heading; not the right choice if the only reason is styling.\n- **`<div>`:** carries no semantic meaning; exposes no landmark — use it only for CSS or JS hooks.",
    "answer": "These three are frequently confused:\n\n- **`<article>`** - independently distributable, self-contained content that still makes sense out of context (a news story, forum post, product card, comment). You can nest articles (e.g. comments inside a post article).\n- **`<section>`** - a thematic grouping of content, typically introduced by a heading. Use it when content belongs together but is *not* independently meaningful. If the only reason is styling, it should not be a `<section>`.\n- **`<div>`** - a *non-semantic* generic block container. It has no meaning and exposes no landmark; use it purely as a styling/layout/JS hook.\n\n**Decision guide:** Does the content stand alone and could be syndicated? -> `<article>`. Is it a meaningful thematic chunk with a heading? -> `<section>`. Neither, just need a box? -> `<div>`.",
    "codeLang": "html",
    "tags": ["semantic", "structure", "article", "section"]
  },
  {
    "id": "html-doctype-quirks-mode",
    "question": "What is the DOCTYPE and what is quirks mode?",
    "short": "**`<!DOCTYPE html>` tells the browser to use standards mode for consistent, spec-compliant rendering.**\n\n- **Standards mode:** triggered by `<!DOCTYPE html>`; the browser follows current HTML/CSS specs.\n- **Quirks mode:** triggered by a missing or invalid doctype; mimics legacy 1990s browser bugs for backward compatibility.\n- **Box-model impact:** in quirks mode `width` includes padding and border (old IE behavior); in standards mode it does not — omitting the doctype causes subtle layout bugs.",
    "answer": "The **DOCTYPE** is the first line of an HTML document. The modern `<!DOCTYPE html>` is not a tag and contains no version - it simply switches the browser into **standards mode**.\n\n**The three rendering modes:**\n- **Standards (no-quirks) mode** - the browser follows the current HTML/CSS specs. This is what you want.\n- **Quirks mode** - triggered by a missing/invalid doctype; the browser mimics non-standard behavior of old browsers for backward compatibility.\n- **Limited-quirks (almost standards) mode** - mostly standards, with a few legacy quirks (notably around inline image box height in table cells).\n\n**Classic quirks-mode differences:**\n- The **box model** reverts to the old IE model (padding/border counted inside `width`).\n- Different handling of `line-height`, percentages, and image spacing.\n\n**Takeaway:** Always include `<!DOCTYPE html>` to guarantee standards mode and avoid subtle cross-browser layout bugs.",
    "code": "<!DOCTYPE html>\n<html lang=\"en\">\n  <head><meta charset=\"utf-8\"></head>\n  <body>...</body>\n</html>",
    "codeLang": "html",
    "tags": ["doctype", "quirks-mode", "rendering", "structure"]
  },
  {
    "id": "html-async-vs-defer",
    "question": "What is the difference between the async and defer attributes on a <script>?",
    "short": "**Both `async` and `defer` download the script in parallel without blocking HTML parsing, but they differ in *when* the script runs.**\n\n- **`async`:** runs as soon as it downloads, possibly mid-parse; order between multiple async scripts is not guaranteed — best for independent snippets like analytics.\n- **`defer`:** waits until parsing finishes, then runs in document order just before `DOMContentLoaded` — best for app code that depends on the DOM or other scripts.\n- **Modules:** `<script type=\"module\">` is deferred by default.",
    "answer": "By default a `<script>` is **parser-blocking**: HTML parsing pauses while the script downloads and executes. `async` and `defer` (valid only on external scripts with `src`) avoid blocking the download.\n\n| Attribute | Download | Execution | Order preserved? |\n|-----------|----------|-----------|------------------|\n| (none) | blocks parsing | immediately, blocks parsing | yes |\n| `async` | parallel, non-blocking | **as soon as downloaded**, may interrupt parsing | **no** |\n| `defer` | parallel, non-blocking | **after parsing completes**, before `DOMContentLoaded` | **yes** |\n\n**When to use which:**\n- **`defer`** - for scripts that depend on the DOM or on each other (most app/bundle code). Safe default.\n- **`async`** - for independent, order-agnostic scripts like analytics or ad tags.\n- **Modules** (`<script type=\"module\">`) are deferred by default.\n\nNote: `async` execution timing is unpredictable relative to DOM readiness, which is why ordered dependencies should use `defer`.",
    "code": "<!-- Runs after HTML is parsed, in order -->\n<script src=\"app.js\" defer></script>\n\n<!-- Independent, runs whenever it arrives -->\n<script src=\"analytics.js\" async></script>",
    "codeLang": "html",
    "tags": ["scripts", "performance", "async", "defer"]
  },
  {
    "id": "html-block-inline-inline-block",
    "question": "What is the difference between block, inline, and inline-block elements?",
    "short": "**Block, inline, and inline-block describe how an element participates in the page's flow and whether it respects `width`/`height`.**\n\n- **Block** (`<div>`, `<p>`):** starts on a new line, fills the available width, and respects all box-model properties.\n- **Inline** (`<span>`, `<a>`):** flows within a line of text; `width` and `height` are ignored and vertical margins have no layout effect.\n- **Inline-block:** flows inline like text but behaves as a block internally — `width`, `height`, and vertical padding/margin all take effect.",
    "answer": "This refers to the **CSS display type**, with sensible defaults set by the HTML element type.\n\n- **Block** (`display: block`) - begins on a new line, stretches to fill the available width, and honors `width`, `height`, and all margins/padding. Examples: `<div>`, `<p>`, `<section>`, `<h1>`.\n- **Inline** (`display: inline`) - flows along with surrounding text on the same line; **ignores `width`/`height`** and top/bottom margins (horizontal padding/margin apply but don't affect line layout cleanly). Examples: `<span>`, `<a>`, `<strong>`, `<em>`.\n- **Inline-block** (`display: inline-block`) - sits inline like text (no forced line break) **but behaves like a block internally**, so `width`, `height`, and vertical padding/margin all work. Great for buttons/badges laid out in a row.\n\n**Note:** these are CSS concepts; any element's display can be overridden in CSS, and modern layouts more often use `flex`/`grid`.",
    "code": ".badge {\n  display: inline-block;\n  width: 80px;      /* respected (would be ignored if inline) */\n  padding: 4px 8px; /* vertical padding now affects layout */\n}",
    "codeLang": "css",
    "tags": ["display", "block", "inline", "layout"]
  },
  {
    "id": "html-data-attributes",
    "question": "What are data-* attributes and when should you use them?",
    "short": "**`data-*` attributes are the standards-compliant way to attach custom private data to an element without inventing non-standard attributes.**\n\n- **JS access:** read and write via `element.dataset` — `data-user-id` maps to `dataset.userId` (kebab → camelCase); values are always strings.\n- **CSS access:** target them with attribute selectors like `[data-state=\"open\"]`.\n- **Good uses:** passing IDs or config from server-rendered HTML to JS, and toggling CSS state; never use them for accessibility info or large data blobs.",
    "answer": "**`data-*` attributes** are a standard way to attach custom, application-specific data to HTML elements without abusing non-standard attributes or class names.\n\n**Access from JavaScript** via the `dataset` property, where `data-user-id` becomes `dataset.userId` (kebab-case maps to camelCase). You can also use `getAttribute('data-user-id')`.\n\n**Good uses:**\n- Passing IDs/config from server-rendered HTML to JS (e.g. `data-product-id`).\n- State flags for JS behavior or CSS via attribute selectors (`[data-state=\"open\"]`).\n\n**Avoid using them for:**\n- Data that has a proper attribute (use `value`, `href`, `title`, etc. instead).\n- **Accessibility** - assistive tech does not read `data-*`; use ARIA/visible text.\n- Large datasets or as a database substitute.",
    "code": "<button data-product-id=\"42\" data-in-stock=\"true\">Buy</button>\n\n<script>\n  const btn = document.querySelector('button');\n  btn.dataset.productId; // \"42\"\n  btn.dataset.inStock;   // \"true\" (always a string)\n</script>",
    "codeLang": "html",
    "tags": ["data-attributes", "dataset", "javascript"]
  },
  {
    "id": "html-localstorage-vs-sessionstorage-vs-cookies",
    "question": "What is the difference between localStorage, sessionStorage, and cookies?",
    "short": "**All three store data on the client, but they differ in lifetime, capacity, and whether the server can see them.**\n\n- **`localStorage`:** persists indefinitely (~5-10 MB); never sent to the server — ideal for UI preferences and non-sensitive caches.\n- **`sessionStorage`:** clears when the tab closes (~5 MB); never sent to the server — ideal for per-tab transient state like multi-step form progress.\n- **Cookies:** small (~4 KB) and sent with every matching HTTP request — use them for auth/session tokens the server must read; secure with `HttpOnly`, `Secure`, and `SameSite`.",
    "answer": "| Feature | localStorage | sessionStorage | Cookies |\n|---------|--------------|----------------|---------|\n| Capacity | ~5-10 MB | ~5-10 MB | ~4 KB |\n| Lifetime | until cleared | until tab closes | `Expires`/`Max-Age` |\n| Scope | per origin | per origin + per tab | per domain/path |\n| Sent to server | no | no | **yes, every request** |\n| API | `localStorage` | `sessionStorage` | `document.cookie` / headers |\n\n**When to use each:**\n- **localStorage** - persistent client-only data: theme, UI preferences, non-sensitive caches.\n- **sessionStorage** - data scoped to a single tab/visit: multi-step form progress, scroll state.\n- **Cookies** - data the **server** needs on each request, especially auth/session tokens. Secure them with `HttpOnly` (blocks JS access, mitigates XSS), `Secure` (HTTPS only), and `SameSite` (mitigates CSRF).\n\n**Security:** Web Storage is always accessible to JS, so it is vulnerable to XSS; never store secrets there. `HttpOnly` cookies are the safer place for session tokens.",
    "code": "localStorage.setItem('theme', 'dark');\nsessionStorage.setItem('step', '2');\n// Cookie set by server:\n// Set-Cookie: sid=abc; HttpOnly; Secure; SameSite=Lax; Max-Age=3600",
    "codeLang": "js",
    "tags": ["web-storage", "cookies", "localstorage", "security"]
  },
  {
    "id": "html-meta-tags-seo",
    "question": "What are meta tags and which ones matter for SEO and rendering?",
    "short": "**`<meta>` tags supply machine-readable metadata in `<head>` that affects rendering, indexing, and social sharing without appearing on the page itself.**\n\n- **Mandatory baseline:** `<meta charset=\"utf-8\">` (encoding) and `<meta name=\"viewport\">` (responsive layout) must always be present.\n- **SEO:** `<title>` is the most important on-page signal; `<meta name=\"description\">` controls the search-result snippet and click-through rate.\n- **Crawler control:** `<meta name=\"robots\">` governs indexing; `<link rel=\"canonical\">` dedupes URLs — the old `keywords` tag is ignored by all major search engines.",
    "answer": "**`<meta>`** elements provide machine-readable metadata that doesn't appear on the page itself but influences rendering, indexing, and how the page is shared.\n\n**Critical for rendering:**\n- `<meta charset=\"utf-8\">` - declares character encoding; should be first in `<head>`.\n- `<meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">` - essential for responsive/mobile layout.\n\n**Important for SEO/sharing:**\n- `<meta name=\"description\">` - the snippet search engines often show (does not directly affect ranking but affects click-through).\n- `<meta name=\"robots\" content=\"index, follow\">` - controls crawling/indexing (`noindex`, `nofollow`).\n- `<title>` - not a meta tag, but the single most important on-page SEO/UX signal.\n- `<link rel=\"canonical\">` - declares the preferred URL to avoid duplicate-content issues.\n\n**Note:** The `keywords` meta tag is obsolete - major search engines ignore it.",
    "code": "<head>\n  <meta charset=\"utf-8\">\n  <meta name=\"viewport\" content=\"width=device-width, initial-scale=1\">\n  <title>Concise, Unique Page Title</title>\n  <meta name=\"description\" content=\"A 150-160 char summary for search results.\">\n  <link rel=\"canonical\" href=\"https://example.com/page\">\n</head>",
    "codeLang": "html",
    "tags": ["meta", "seo", "head", "viewport"]
  },
  {
    "id": "html-open-graph",
    "question": "What is Open Graph and why is it used?",
    "short": "**Open Graph `og:` meta tags control how a page appears when shared on social platforms, producing a rich preview card instead of a bare URL.**\n\n- **Core tags:** `og:title`, `og:description`, `og:image` (absolute URL, ~1200×630 px recommended), and `og:url` make up the minimum useful set.\n- **Twitter/X:** uses its own `twitter:card` tags but falls back to Open Graph if they are absent.\n- **Page rendering:** these tags have no effect on how the page looks to visitors — they are read only by link-preview crawlers.",
    "answer": "**Open Graph Protocol** is a set of `<meta property=\"og:*\">` tags (originally from Facebook) that social networks and chat apps read to build a **rich preview card** when your link is shared.\n\n**Core tags:**\n- `og:title` - title shown on the card (can differ from `<title>`).\n- `og:description` - the preview blurb.\n- `og:image` - the preview thumbnail (specify an absolute URL; recommend ~1200x630).\n- `og:url` - the canonical URL of the page.\n- `og:type` - e.g. `website`, `article`.\n\n**Twitter/X** has a parallel set (`twitter:card`, `twitter:image`) but falls back to Open Graph when those are absent.\n\n**Why it matters:** Without these tags, shared links render as plain text/URLs; with them, links get attractive cards that improve click-through and brand presentation. They have no effect on the page's own rendering.",
    "code": "<meta property=\"og:title\" content=\"My Article\">\n<meta property=\"og:description\" content=\"Short, compelling summary.\">\n<meta property=\"og:image\" content=\"https://example.com/cover.png\">\n<meta property=\"og:url\" content=\"https://example.com/article\">\n<meta property=\"og:type\" content=\"article\">",
    "codeLang": "html",
    "tags": ["open-graph", "seo", "social", "meta"]
  },
  {
    "id": "html-accessibility-basics",
    "question": "What are the core principles of building accessible HTML?",
    "short": "**Building accessible HTML means using native elements, providing text alternatives, ensuring keyboard operability, and using ARIA only as a last resort to fill genuine gaps.**\n\n- **Prefer native elements:** a real `<button>`, `<a>`, or `<input>` is focusable, keyboard-operable, and correctly announced by default — never rebuild them from `<div>`s.\n- **Text alternatives:** every `<img>` needs a purposeful `alt`; every form control needs a `<label>`; icon-only buttons need an `aria-label`.\n- **Structure and contrast:** maintain a single `<h1>`, never skip heading levels, use semantic landmarks, and ensure sufficient color contrast — never convey meaning by color alone.\n- **ARIA last:** the first rule of ARIA is *don't use it if a native element will do*; bad ARIA is worse than no ARIA.",
    "answer": "Accessibility (a11y) means content is usable by people relying on screen readers, keyboards, magnifiers, etc. Core principles:\n\n- **Prefer native HTML** - a real `<button>`, `<a>`, `<input>` is focusable, keyboard-operable, and announced correctly out of the box. Don't rebuild these with `<div>`s.\n- **Text alternatives** - meaningful `alt` on `<img>`; `<label>`s tied to form fields; accessible names for icon buttons.\n- **Keyboard operability** - every interactive control must be reachable and operable with Tab/Enter/Space; preserve a logical focus order; provide visible focus styles.\n- **Logical structure** - one `<h1>`, no skipped heading levels, and proper landmarks (`<nav>`, `<main>`, etc.).\n- **Color & contrast** - sufficient contrast; never use color alone to convey meaning.\n- **ARIA last** - the first rule of ARIA is *don't use ARIA if a native element will do*; **no ARIA is better than bad ARIA**.\n\nAccessibility overlaps heavily with SEO and general code quality, so it's expected knowledge for experienced roles.",
    "codeLang": "html",
    "tags": ["accessibility", "a11y", "aria", "keyboard"]
  },
  {
    "id": "html-aria-roles-when",
    "question": "What is ARIA and when should you use it?",
    "short": "**ARIA adds `role`, state, and property attributes that describe UI to assistive technologies when native HTML cannot.**\n\n- **Three categories:** roles (what it *is* — `role=\"dialog\"`), states (dynamic conditions — `aria-expanded`, `aria-checked`), and properties (relationships — `aria-label`, `aria-describedby`).\n- **First rule:** don't use ARIA if a native element already provides the semantics — `<button>` beats `<div role=\"button\">` every time.\n- **ARIA changes only announcements, not behavior:** you must wire up keyboard handling and keep state attributes in sync with JS yourself.",
    "answer": "**ARIA** is a set of attributes that describe the **role**, **state**, and **properties** of UI to assistive technologies, mainly for custom or dynamic widgets where native HTML falls short.\n\n**Three categories:**\n- **Roles** - what an element *is*: `role=\"tab\"`, `role=\"dialog\"`, `role=\"alert\"`.\n- **States** - dynamic conditions: `aria-expanded`, `aria-checked`, `aria-disabled`.\n- **Properties** - relationships/labels: `aria-label`, `aria-labelledby`, `aria-describedby`, `aria-controls`.\n\n**The rules of ARIA:**\n1. **Don't use ARIA if a native element/attribute provides the semantics** (use `<button>`, not `<div role=\"button\">`).\n2. **Don't change native semantics** unless you must.\n3. All interactive ARIA controls must be **keyboard usable**.\n4. **Don't use `role=\"presentation\"` or `aria-hidden=\"true\"` on focusable elements.**\n\n**Key point:** ARIA changes only *how things are announced* - it adds **no behavior**. You must wire up keyboard handling and state updates yourself. Hence: *no ARIA is better than bad ARIA.*\n\nUseful live-region attributes: `aria-live=\"polite\"`/`\"assertive\"` announce dynamic content changes (toasts, validation messages).",
    "code": "<!-- Custom toggle: native button + ARIA state you must update in JS -->\n<button aria-expanded=\"false\" aria-controls=\"menu\">Menu</button>\n<ul id=\"menu\" hidden>...</ul>",
    "codeLang": "html",
    "tags": ["aria", "accessibility", "roles", "a11y"]
  },
  {
    "id": "html-img-alt-text",
    "question": "Why is the alt attribute important and how should you write it?",
    "short": "**The `alt` attribute gives an image a text alternative read by screen readers, shown when the image fails to load, and indexed by search engines.**\n\n- **Informative images:** describe the purpose or meaning, not every pixel; don't start with \"image of\" — that role is implicit.\n- **Decorative images:** use `alt=\"\"` (empty but present) so screen readers skip them — omitting `alt` entirely is wrong and may cause the filename to be announced.\n- **Complex content:** for charts or diagrams, provide a longer description nearby or via `aria-describedby`.",
    "answer": "The **`alt`** attribute on `<img>` is the image's text alternative and is required for accessible, robust pages.\n\n**It serves three roles:**\n1. **Accessibility** - screen readers announce it; without it they may read the filename or skip the image.\n2. **Fallback** - displayed if the image fails to load.\n3. **SEO** - search engines use it to understand and index images.\n\n**How to write good alt text:**\n- Describe the **purpose/meaning** in context, not every pixel. For a logo link: `alt=\"Acme - home\"`.\n- Keep it concise; don't start with \"image of\" (the role is already known).\n- **Decorative images** that add no information should use **empty alt** (`alt=\"\"`) so screen readers skip them. Omitting `alt` entirely is *not* the same - that can cause the filename to be read.\n- For images conveying complex data (charts), provide a longer description nearby or via `aria-describedby`.",
    "code": "<!-- Informative -->\n<img src=\"chart.png\" alt=\"Q3 revenue up 12% over Q2\">\n\n<!-- Decorative: skipped by screen readers -->\n<img src=\"divider.svg\" alt=\"\">",
    "codeLang": "html",
    "tags": ["accessibility", "images", "alt", "seo"]
  },
  {
    "id": "html-label-form-association",
    "question": "Why are <label> elements important and how do you associate them with inputs?",
    "short": "**A `<label>` gives a form control an accessible name and a larger click/tap target.**\n\n- **Explicit association:** `<label for=\"id\">` matched to the input's `id` — works even when not adjacent; preferred approach.\n- **Implicit association:** wrap the input inside the `<label>` element — both methods are equivalent.\n- **Grouping:** wrap related radios or checkboxes in `<fieldset>` with a `<legend>` so screen readers announce the group name alongside each option; placeholder text is never a substitute for a label.",
    "answer": "A **`<label>`** ties human-readable text to a form control, which is essential for usability and accessibility.\n\n**Benefits:**\n- Screen readers announce the label when the control is focused (provides its *accessible name*).\n- Clicking/tapping the label focuses or toggles the control - a bigger hit area, important for checkboxes/radios and touch.\n\n**Two ways to associate:**\n1. **Explicit** - `<label for=\"email\">` matches `<input id=\"email\">`. Preferred; works even when not adjacent.\n2. **Implicit** - wrap the input inside the label.\n\n**Related grouping:** wrap a set of related controls (e.g. radio options) in a **`<fieldset>`** with a **`<legend>`** to give the group an accessible name. Placeholder text is *not* a substitute for a label - it disappears on input and has poor contrast/AT support.",
    "code": "<!-- Explicit (preferred) -->\n<label for=\"email\">Email</label>\n<input id=\"email\" type=\"email\" name=\"email\">\n\n<!-- Grouped controls -->\n<fieldset>\n  <legend>Shipping speed</legend>\n  <label><input type=\"radio\" name=\"speed\" value=\"std\"> Standard</label>\n  <label><input type=\"radio\" name=\"speed\" value=\"exp\"> Express</label>\n</fieldset>",
    "codeLang": "html",
    "tags": ["forms", "accessibility", "label", "fieldset"]
  },
  {
    "id": "html-input-types-native-validation",
    "question": "What HTML5 input types and native validation features exist, and why use them?",
    "short": "**HTML5 input types and validation attributes give browsers the information to show appropriate keyboards, date pickers, and built-in validation messages with no JavaScript.**\n\n- **Useful types:** `email`, `url`, `number`, `date`, `tel`, `range`, `color` — each triggers the correct mobile keyboard or picker and enables automatic format checking.\n- **Validation attributes:** `required`, `pattern`, `min`/`max`, `step`, `minlength`/`maxlength` block invalid submissions and surface accessible error messages for free.\n- **Server-side always required:** client-side validation is a UX convenience only — it can be bypassed, so always re-validate on the server.",
    "answer": "HTML5 expanded `<input type>` well beyond `text`, and added **native (constraint) validation** so common checks need no JavaScript.\n\n**Useful input types:** `email`, `url`, `tel`, `number`, `range`, `date`/`time`/`datetime-local`, `color`, `search`, `password`, `file`. On mobile these trigger the **appropriate on-screen keyboard** and pickers.\n\n**Validation attributes:**\n- `required` - field must be filled.\n- `type=\"email\"`/`\"url\"` - format checking.\n- `pattern=\"...\"` - regex constraint.\n- `min`, `max`, `step` - numeric/date bounds.\n- `minlength`, `maxlength` - length bounds.\n\n**Why use it:**\n- **Free, accessible** validation UI and messages, before any JS loads.\n- Style states with CSS pseudo-classes `:valid`, `:invalid`, `:required`.\n- Hook into the **Constraint Validation API** (`checkValidity()`, `setCustomValidity()`) for custom messages.\n\n**Caveat:** client-side validation is for UX only - **always re-validate on the server**, since it can be bypassed.",
    "code": "<form>\n  <input type=\"email\" required placeholder=\"you@site.com\">\n  <input type=\"number\" min=\"1\" max=\"10\" step=\"1\" required>\n  <input type=\"text\" pattern=\"[A-Za-z]{3,}\" title=\"Letters only, 3+\">\n  <button>Submit</button>\n</form>",
    "codeLang": "html",
    "tags": ["forms", "validation", "input-types", "html5"]
  },
  {
    "id": "html-get-vs-post-form",
    "question": "What is the difference between GET and POST methods on a form?",
    "short": "**`GET` appends form data to the URL; `POST` sends it in the request body — the choice is about semantics and where data travels, not security by itself.**\n\n- **GET:** data visible in the URL, bookmarkable and cacheable; use for safe, idempotent reads like search queries — never for passwords or sensitive data.\n- **POST:** data in the request body, not cached or bookmarked; supports large payloads and file uploads via `enctype=\"multipart/form-data\"`; use for state-changing actions.\n- **Security note:** POST is not encryption — only **HTTPS** protects data in transit regardless of method.",
    "answer": "The form `method` attribute controls how submitted data is sent.\n\n**GET:**\n- Data is appended to the URL as a **query string** (`?q=...&page=2`).\n- **Bookmarkable / shareable**, cached, and shown in browser history.\n- Limited length; **never** for passwords or sensitive data (it leaks into URLs, logs, history).\n- Semantically for **safe, idempotent reads** (search, filters).\n\n**POST:**\n- Data is sent in the **request body**, not the URL.\n- No practical size limit; supports file uploads (`enctype=\"multipart/form-data\"`).\n- Not cached or bookmarked; re-submitting may prompt the user.\n- Semantically for actions that **change state** (create, update, delete) and for sensitive data.\n\n**Note:** POST is not encryption - only **HTTPS** protects data in transit. The choice is about semantics and where data travels, not security by itself.",
    "code": "<form action=\"/search\" method=\"get\">...</form>   <!-- read -->\n<form action=\"/users\" method=\"post\">...</form>   <!-- create -->",
    "codeLang": "html",
    "tags": ["forms", "http", "get", "post"]
  },
  {
    "id": "html-responsive-images-srcset-picture",
    "question": "How do srcset/sizes and the <picture> element enable responsive images?",
    "short": "**`srcset`+`sizes` and `<picture>` solve two different responsive-image problems: one lets the browser pick the best resolution; the other lets the developer swap entirely different images.**\n\n- **`srcset`+`sizes` (resolution switching):** same image at multiple widths — the browser picks the smallest file that looks sharp for the device's viewport and DPR.\n- **`<picture>`+`<source>` (art direction/format):** use when you need a different crop at different breakpoints, or to offer modern formats (AVIF/WebP) with a JPEG fallback.\n- **Required `<img>`:** inside `<picture>` the `<img>` is mandatory — it is the fallback and carries the `alt` text.",
    "answer": "Two complementary mechanisms serve the right image to each device, saving bandwidth and improving performance.\n\n**1. `srcset` + `sizes` (resolution switching)** - same image, different sizes; the **browser chooses**.\n- `srcset` lists candidate files with width descriptors (`480w`, `800w`).\n- `sizes` tells the browser how wide the image will *render* at various breakpoints, so it can pick the smallest sufficient file (also accounts for device pixel ratio).\n\n**2. `<picture>` + `<source>` (art direction & format negotiation)** - *you* control which image is used.\n- Use `media` queries to serve a cropped/different image at different breakpoints.\n- Use `type` to offer modern formats (AVIF/WebP) with a fallback `<img>` for older browsers.\n- The `<img>` inside `<picture>` is required - it's the fallback and carries `alt`.\n\n**Key distinction:** `srcset`/`sizes` = browser optimizes the *same* image; `<picture>` = developer dictates *different* images/formats.",
    "code": "<!-- Resolution switching -->\n<img src=\"m.jpg\"\n     srcset=\"s.jpg 480w, m.jpg 800w, l.jpg 1200w\"\n     sizes=\"(max-width: 600px) 100vw, 50vw\"\n     alt=\"...\">\n\n<!-- Art direction + modern format -->\n<picture>\n  <source type=\"image/avif\" srcset=\"hero.avif\">\n  <source media=\"(max-width: 600px)\" srcset=\"hero-mobile.jpg\">\n  <img src=\"hero.jpg\" alt=\"...\">\n</picture>",
    "codeLang": "html",
    "tags": ["images", "responsive", "srcset", "picture", "performance"]
  },
  {
    "id": "html-lazy-loading",
    "question": "What is lazy loading and how do you enable it natively?",
    "short": "**`loading=\"lazy\"` defers fetching off-screen images and iframes until the user scrolls near them, improving initial load time and saving bandwidth.**\n\n- **When to use:** apply to below-the-fold images and third-party embeds; `loading=\"eager\"` (the default) loads immediately.\n- **Never lazy-load the hero:** the above-the-fold / LCP image should use `loading=\"eager\"` and `fetchpriority=\"high\"` — deferring it directly hurts Core Web Vitals.\n- **Prevent layout shift:** always set explicit `width` and `height` (or `aspect-ratio`) on lazy images so the browser reserves space before the image loads.",
    "answer": "**Lazy loading** delays fetching resources that aren't yet visible, so the browser prioritizes above-the-fold content - improving **initial load time, LCP, and data usage**.\n\n**Native support** via the `loading` attribute on `<img>` and `<iframe>`:\n- `loading=\"lazy\"` - defer until the element nears the viewport.\n- `loading=\"eager\"` - load immediately (the default).\n\n**Best practices:**\n- **Do NOT lazy-load above-the-fold / LCP images** (e.g. the hero) - it delays the most important paint. Mark those `loading=\"eager\"` and consider `fetchpriority=\"high\"`.\n- Always set explicit `width`/`height` (or `aspect-ratio`) to reserve space and avoid **layout shift (CLS)**.\n- For finer control or older browsers, use the **Intersection Observer API** in JS.\n\nNative lazy loading is the simplest approach and is broadly supported in modern browsers.",
    "code": "<!-- Below-the-fold image -->\n<img src=\"photo.jpg\" alt=\"...\" width=\"800\" height=\"600\" loading=\"lazy\">\n\n<!-- Hero / LCP image: load eagerly with high priority -->\n<img src=\"hero.jpg\" alt=\"...\" loading=\"eager\" fetchpriority=\"high\">",
    "codeLang": "html",
    "tags": ["performance", "images", "lazy-loading", "core-web-vitals"]
  },
  {
    "id": "html-iframe-sandbox",
    "question": "What is an <iframe> and how does the sandbox attribute secure it?",
    "short": "**An `<iframe>` embeds another HTML document in its own browsing context; the `sandbox` attribute applies a deny-by-default policy to restrict what the embedded page can do.**\n\n- **Bare `sandbox`:** no scripts, no forms, no popups, and the frame is treated as a unique opaque origin — maximum restriction.\n- **Re-enable selectively:** add only the tokens you need, e.g. `sandbox=\"allow-scripts allow-forms\"`.\n- **Critical caveat:** combining `allow-scripts` and `allow-same-origin` for untrusted content lets the frame remove its own sandbox — avoid that pairing.",
    "answer": "An **`<iframe>`** embeds an independent browsing context (another document) within the page - used for third-party widgets, maps, videos, payment forms, etc.\n\n**Risks:** embedded content can run scripts, submit forms, trigger navigation, or open popups. The **`sandbox`** attribute applies a strict default-deny policy.\n\n- **`sandbox` with no value** - maximum restriction: no scripts, no forms, no popups, treated as a unique opaque origin (no same-origin access), no top-level navigation.\n- **Re-enable selectively** with space-separated tokens:\n  - `allow-scripts` - permit JavaScript.\n  - `allow-forms` - permit form submission.\n  - `allow-same-origin` - keep its real origin.\n  - `allow-popups`, `allow-top-navigation`, etc.\n\n**Caution:** combining `allow-scripts` **and** `allow-same-origin` lets the frame remove its own sandboxing, so avoid that pairing for untrusted content.\n\n**Related security attributes:** `referrerpolicy`, `loading=\"lazy\"`, and `allow` (Permissions Policy) for features like camera/fullscreen. Server-side, `X-Frame-Options`/`frame-ancestors` (CSP) control whether *your* page can be framed by others (clickjacking defense).",
    "code": "<iframe src=\"https://widget.example.com\"\n        sandbox=\"allow-scripts allow-forms\"\n        loading=\"lazy\"\n        title=\"Embedded widget\"></iframe>",
    "codeLang": "html",
    "tags": ["iframe", "security", "sandbox", "embedding"]
  },
  {
    "id": "html-canvas-vs-svg",
    "question": "What is the difference between <canvas> and SVG?",
    "short": "**Canvas and SVG are opposite rendering models: canvas is an immediate-mode raster bitmap drawn via JS; SVG is a retained-mode vector DOM.**\n\n- **Canvas:** shapes are just pixels — no DOM per shape, no CSS styling, not accessible by default, and resolution-dependent; best for games, large datasets, or pixel manipulation.\n- **SVG:** each shape is a DOM element — styleable with CSS, bindable with events, resolution-independent, and accessible via `<title>`/ARIA; best for icons, charts, and diagrams.\n- **Rule of thumb:** few interactive scalable elements → SVG; many objects or high-frequency rendering → canvas.",
    "answer": "Both render graphics in the browser but differ fundamentally.\n\n**`<canvas>` (raster / immediate mode):**\n- A pixel bitmap you draw onto with the **2D/WebGL JavaScript API**.\n- Shapes are **not** in the DOM - once drawn, they're just pixels (no per-shape events, must redraw to change).\n- **Resolution-dependent** - scaling up blurs/pixelates.\n- Best for: games, charts with thousands of points, image/video processing, real-time pixel work.\n\n**SVG (vector / retained mode):**\n- XML markup; each shape is a **DOM element** you can style with CSS and attach event listeners to.\n- **Resolution-independent** - scales crisply at any size/DPI.\n- **Accessible** (can include `<title>`/`<desc>`, ARIA) and easily animated/styled.\n- Best for: icons, logos, diagrams, interactive charts, anything that must scale or be styled.\n\n**Rule of thumb:** few, interactive, scalable elements -> **SVG**; many objects or pixel-level/high-frequency rendering -> **canvas** (DOM overhead would make SVG slow).",
    "code": "<!-- SVG: vector, in the DOM, styleable -->\n<svg viewBox=\"0 0 100 100\" role=\"img\" aria-label=\"Circle\">\n  <circle cx=\"50\" cy=\"50\" r=\"40\" fill=\"tomato\"/>\n</svg>\n\n<!-- Canvas: pixels drawn via JS -->\n<canvas id=\"c\" width=\"100\" height=\"100\"></canvas>\n<script>\n  const ctx = document.getElementById('c').getContext('2d');\n  ctx.fillStyle = 'tomato';\n  ctx.beginPath(); ctx.arc(50, 50, 40, 0, Math.PI*2); ctx.fill();\n</script>",
    "codeLang": "html",
    "tags": ["canvas", "svg", "graphics", "performance"]
  },
  {
    "id": "html-what-is-the-dom",
    "question": "What is the DOM and how does it relate to HTML?",
    "short": "**The DOM is the browser's live, in-memory tree representation of a parsed HTML document — HTML is the static source text, the DOM is the dynamic object model JavaScript reads and mutates.**\n\n- **Parsing gap:** the browser fixes invalid markup and injects implied elements (e.g. `<tbody>`), so \"View Source\" (original HTML) can differ from \"Inspect Element\" (current DOM).\n- **JS interaction:** DOM APIs like `querySelector`, `createElement`, and `addEventListener` are how scripts read and change the page dynamically.\n- **Performance note:** DOM manipulation triggers reflows/repaints — batch updates to avoid layout thrashing.",
    "answer": "The **DOM (Document Object Model)** is a **language-neutral, tree-structured object representation** of a document that the browser builds while parsing the HTML. Each element, attribute, and text node becomes an object (a node) that scripts can query and manipulate.\n\n**HTML vs DOM:**\n- **HTML** is the *static source* - the markup you write/send.\n- The browser *parses* it into the **DOM tree** - a live structure in memory.\n- They can diverge: the browser **fixes invalid markup**, injects implied elements (e.g. `<tbody>`), and JS can add/remove/modify nodes after load. So \"View Source\" (original HTML) can differ from \"Inspect\" (current DOM).\n\n**Why it matters:**\n- JS uses DOM APIs (`getElementById`, `querySelector`, `createElement`, `addEventListener`) to read and change the page dynamically.\n- DOM manipulation can be expensive; reflows/repaints are why frameworks (React's virtual DOM, etc.) batch updates.\n- The **CSSOM** (styles) combines with the DOM into the **render tree** the browser paints.",
    "code": "// HTML source: <p id=\"x\">Hi</p>\nconst p = document.getElementById('x'); // a DOM node\np.textContent = 'Updated';               // mutates the live DOM\ndocument.body.appendChild(document.createElement('div'));",
    "codeLang": "js",
    "tags": ["dom", "javascript", "rendering", "parsing"]
  },
  {
    "id": "html-head-vs-body",
    "question": "What belongs in <head> versus <body>?",
    "short": "**`<head>` holds metadata not rendered as content (title, charset, styles, scripts); `<body>` holds the visible, interactive content the user sees.**\n\n- **What goes in `<head>`:** `<title>`, `<meta>` tags (charset, viewport, description, Open Graph), `<link>` (stylesheets, canonical, preconnect), and `<script>` tags (often deferred).\n- **What goes in `<body>`:** all structural and semantic content — headings, text, images, forms, links, and interactive elements.\n- **Order matters:** `<meta charset>` must appear early so text is decoded correctly; render-blocking CSS in `<head>` prevents a flash of unstyled content.",
    "answer": "Every HTML document splits into two top-level sections inside `<html>`:\n\n**`<head>` - metadata and resources (not rendered as content):**\n- `<title>` - tab/bookmark/SEO title.\n- `<meta>` - charset, viewport, description, robots, Open Graph.\n- `<link>` - stylesheets, `canonical`, `preconnect`/`preload`, favicons.\n- `<script>` - though often deferred or placed at the end of `<body>`.\n- `<style>` - inline CSS.\n\n**`<body>` - the visible document:**\n- All structural/semantic content: headings, text, images, forms, links, etc.\n\n**Why the split matters:**\n- `<meta charset>` must appear early in `<head>` so the browser decodes text correctly.\n- Render-blocking CSS in `<head>` avoids a flash of unstyled content; scripts are commonly deferred so they don't block parsing.\n- Resource hints (`preconnect`, `preload`, `dns-prefetch`) in `<head>` improve load performance.",
    "code": "<html lang=\"en\">\n  <head>\n    <meta charset=\"utf-8\">\n    <title>Page</title>\n    <link rel=\"stylesheet\" href=\"styles.css\">\n  </head>\n  <body>\n    <h1>Visible content</h1>\n  </body>\n</html>",
    "codeLang": "html",
    "tags": ["structure", "head", "body", "metadata"]
  }
]
